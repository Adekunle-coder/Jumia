const slideImage = document.querySelectorAll('.slide_image')
const slideContainer = document.querySelectorAll('.slider_section')
const prevButton = document.querySelectorAll('.prev-btn')
const nextButton = document.querySelectorAll('.next-btn')
const navigation_dots = document.querySelectorAll('.navigation_dots')

// Set Up The Slider images position 

// function init(){
// 	slideImage.forEach((img, i) => {
// 	 img.style.left = 
// 	});

// }

// init()
